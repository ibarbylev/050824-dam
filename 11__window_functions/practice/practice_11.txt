-- 1. Вывести список model_name всех самолетов (airliners) и количество всех самолетов в таблице.
-- без OVER()
-- c OVER()

/* 2. Изменить предыдущий запрос таким образом, чтобы для каждого
      типа самолетов (model_name) выводилось количество самолетов этого типа.
      Подсказка: добавить partition by model_name в over(). */

/* 3. Работаем с таблицей tickets.
      Написать запрос, который выводит список client_id и количество билетов у каждого клиента. */

/* 4. Изменить запрос таким образом, чтобы вместо client_id выводилось имя клиента из таблицы clients */

/* 5. Вывести список сервисных классов со средней стоимостью билета в каждом классе. */

/* 6.Вывести список сервисных классов со средней ценой в каждой классе и
    рангом каждого класса в зависимости от средней цены в сервисном классе. */

/* 7. Вывести количество билетов для каждой поездки. */

/* 8. Вывести топ 2 поездок (trips) по их средней стоимости.
      Поездка может состоять из нескольких перелетов
      (поэтому на каждую поездку может приходиться больше одного билета). */
